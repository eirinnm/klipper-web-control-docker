## rule to restart klipper when the printer is connected via usb.
## may need to edit idVendor and idProduct (use lsusb to find these for your hardware).
## copy this file to /etc/udev/rules.d/
## then reload with sudo udevadm control --reload
SUBSYSTEM=="usb", ATTRS{idVendor}=="1d50", ATTRS{idProduct}=="614e", ACTION=="add", RUN+="/usr/bin/docker exec --user klippy klipper /bin/sh -c '/bin/echo RESTART > /tmp/printer'"
